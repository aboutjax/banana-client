import React from 'react';
import Loader from './loader'

const ExportingLoader = () => (

    <div className='c-exporting-loader'>
      <Loader className="dark"/>
      <h3>Exporting chart</h3>
    </div>

)

export default ExportingLoader
